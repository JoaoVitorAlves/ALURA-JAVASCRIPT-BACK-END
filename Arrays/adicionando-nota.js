const notas = [10, 6, 8];
notas.push(7);//push: função para adicionar um elemento a um array, sempre vai ser o ultimo

console.log(notas)

let media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length

console.log(media)